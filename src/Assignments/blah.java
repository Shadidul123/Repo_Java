package Assignments;

public class blah {
    public static void main(String[] args) {
        int[] numbers = {2, 4, 1, 5};
        int numSize = numbers.length;//return value should be int = 3;

        int maxValue = 5;             //assume found max value;

        // if numbers[0] = 1 or numbers[0] = 2 or numbers[


        //for (int i = 0; i < numSize; i++) {
            //for (int c = i; c <= i; c++) {
        for(int i =0; i<=0; i++){
                for (int j = 1; j < maxValue; j++) {
                    if (numbers[i] == j) {
                        System.out.println(j);
                    }

                }
            }
        }
    }


